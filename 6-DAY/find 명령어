find 명령어

1. 사용자의 홈디렉토리에서 .txt 파일을 모두 검색하여 총 파일의 수를 출력한다.
  $ sudo find ~ -name '*.txt' | wc -l

2. 사용자의 홈디렉토리에서 .txt 파일을 모두 검색하여 Hello 문자열을 추가하는 쉘을 실행한다.
  $ sudo find ~ -name '*.txt' -exec  sh -c "echo Hello >> {}" \;
